# PR Template (Minimal)

## PR Goal (one sentence)

Record the 0.3.0 hard-cut CLI integration lifecycle release prep and verification.

## Commit Narrative

- Commit 1: `refactor: hard-cut cli integration lifecycle`
- Commit 2: `docs: record 0.3.0 hard-cut release verification`

## Regression Test Gate

### Most likely regression surface

- CLI integration install/uninstall behavior, legacy Claude detection, read-only command behavior after removing runtime auto-heal, and diagnostics fidelity for OpenClaw linked/enabled state.

### Verification method (choose at least one)

- [x] `node --test test/init-dry-run.test.js test/cli-hard-cut.test.js test/status.test.js test/init-uninstall.test.js test/diagnostics.test.js test/doctor.test.js` => PASS
- [x] `node --test test/diagnostics.test.js test/openclaw-session-plugin.test.js test/openclaw-hook.test.js` => PASS
- [x] `npm run ci:local` => PASS
- [x] `node scripts/acceptance/npm-install-smoke.cjs` => PASS
- [ ] Published package smoke: `VIBEUSAGE_RUN_NPX=1 node scripts/acceptance/npm-install-smoke.cjs`

### Uncovered scope

- Real `npm publish`, GitHub Actions `CI`, and post-publish `npx` smoke were not run in this turn.

## Notes

- Release intended as `vibeusage@0.3.0`.
- This is a hard-cut release: `init` is the only supported command that mutates local integration config.
- Older Claude installs that only configured `SessionEnd` must re-run `vibeusage init`.
- Review coverage: one reviewer reported no blocking issues; a second reviewer flagged missing OpenClaw `linked`/`enabled` propagation in diagnostics, which was fixed before close-out.
