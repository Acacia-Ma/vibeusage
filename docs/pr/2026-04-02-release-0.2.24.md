# PR Template (Minimal)

## PR Goal (one sentence)

Record the 0.2.24 release notes and regression verification after publishing.

## Commit Narrative

- Commit 1: `release: bump version to 0.2.24`
- Commit 2: `docs: record 0.2.24 release verification`

## Regression Test Gate

### Most likely regression surface

- Claude Code auto-sync install/upgrade behavior and npm release packaging.

### Verification method (choose at least one)

- [x] `node --test test/cli-auto-heal.test.js test/status.test.js test/init-uninstall.test.js` => PASS
- [x] `npm test` => PASS
- [x] `node scripts/acceptance/npm-install-smoke.cjs` => PASS
- [ ] Published package smoke: `VIBEUSAGE_RUN_NPX=1 node scripts/acceptance/npm-install-smoke.cjs`

### Uncovered scope

- Real `npm publish` and post-publish `npx` smoke are blocked until npm auth is restored in this environment.

## Notes

- Release intended as `vibeusage@0.2.24`.
- npm auth status before publish attempt: `npm whoami` => `401 Unauthorized`.
- Local pre-publish packaging smoke passed: `node scripts/acceptance/npm-install-smoke.cjs`.
