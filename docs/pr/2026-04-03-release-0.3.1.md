# PR Template (Minimal)

## PR Goal (one sentence)

Record the 0.3.1 patch release prep for current `main`.

## Commit Narrative

- Commit 1: `release: 0.3.1`

## Regression Test Gate

### Most likely regression surface

- CLI packaging, OpenCode SQLite ingest + diagnostics, and release artifact integrity after carrying the InsForge SDK baseline bump on current `main`.

### Verification method (choose at least one)

- [x] `npm run ci:local` => PASS
- [x] `node --test test/sync-opencode-sqlite.test.js test/rollout-parser.test.js test/diagnostics.test.js test/status.test.js test/doctor.test.js test/opencode-usage-audit.test.js` => PASS
- [x] `node scripts/acceptance/npm-install-smoke.cjs` => PASS
- [ ] Published package smoke: `VIBEUSAGE_RUN_NPX=1 node scripts/acceptance/npm-install-smoke.cjs`

### Uncovered scope

- Real `npm publish`, GitHub Actions `CI`, and post-publish `npx` smoke were not run in this turn because this session does not have npm publish auth and did not push `main`.

## Notes

- Release intended as `vibeusage@0.3.1`.
- This release carries the OpenCode SQLite support finalization from current `main`, including SQLite-backed sync/audit and runtime health visibility in `status`, `diagnostics`, and `doctor`.
- Current npm registry state is still `vibeusage@0.3.0`; publishing `0.3.1` is expected to happen through `.github/workflows/release.yml` after pushing `main`.
