# Design: SSOT foundations

## Domain matrix

| Domain | Authoritative source | Derived layers | Readers |
| --- | --- | --- | --- |
| `auth/session` | InsForge persistent session storage (`dashboard/src/lib/insforge-client.ts`) | soft/session-expired UI markers only | `App.jsx`, auth hooks, API retry flow |
| `viewer identity` | `vibeusage-viewer-identity` endpoint | nullable UI placeholder state | `current-identity.ts`, `DashboardPage.jsx` |
| `model identity` | backend canonical `model_id` contract | display labels, charts, pricing labels | usage summary/model breakdown/dashboard panels |
| `copy registry` | `dashboard/src/content/copy.csv` + shared parser | runtime map, validator report, Vite meta injection | `copy.ts`, `validate-copy-registry.cjs`, `vite.config.js` |
| `runtime defaults` | shared runtime defaults module | CLI config resolution, dashboard config, smoke scripts | `runtime-config.js`, `config.ts`, ops/acceptance scripts |
| `function slugs` | shared function contract module | dashboard API client, CLI API client | `dashboard/src/lib/vibeusage-api.ts`, `src/lib/vibeusage-api.js` |
| `dashboard cache` | shared dashboard cache protocol | hook-local view models only | usage/trend/heatmap/model-breakdown hooks |
| `usage aggregate` | PostgreSQL + backend aggregation | frontend rendering only | usage summary/daily/breakdown/heatmap consumers |

## Strict cutover rules

1. No new compatibility store for auth/session.
2. `model_id` is canonical machine identity. `model` remains display label only.
3. Local cache is provenance-tagged derived data only and must never redefine server truth.
4. Shared parser/default/slug modules are the only place allowed to define those contracts.

## Backend convergence note

Model normalization, canonical identity resolution, and alias timeline rules now converge through `insforge-src/shared/usage-model-core.js`. The ESM layer consumes a mirrored `.mjs` artifact whose contents are guardrailed to stay byte-for-byte identical, while CJS and ESM wrappers remain thin export layers only.

Pricing resolution, cost computation, billable-total resolution, shared totals helpers, and pricing bucket key parsing now follow the same pattern through `insforge-src/shared/pricing-core.js` and `insforge-src/shared/usage-metrics-core.js`, again with mirrored `.mjs` artifacts for the ESM edge layer.

Runtime env parsing, request base URL resolution, numeric coercion, and source normalization now converge through `insforge-src/shared/env-core.js` and `insforge-src/shared/runtime-primitives-core.js`. CJS and ESM wrappers for `env`, `date`, `logging`, `numbers`, and `source` now consume those cores instead of carrying separate helper implementations.

JWT parsing, HS256 verification, bearer-to-user resolution, and public access-context rules now converge through `insforge-src/shared/auth-core.js`. Public share token normalization, public profile resolution, and public visibility state transitions now converge through `insforge-src/shared/public-sharing-core.js`. CJS and ESM wrappers for `auth`, `public-view`, and `public-visibility` now provide only environment/client hashing adapters on top of those cores.

Registration-cutoff and entitlement-based pro status resolution now converge through `insforge-src/shared/pro-status-core.js`. Shared HTTP response helpers (`corsHeaders`, `handleOptions`, `json`, `requireMethod`, `readJson`) now converge through `insforge-src/shared/http-core.js`. CJS and ESM wrappers for `pro-status` and `http` are now thin export layers over those cores.

Canary model filtering, slow-query debug payload construction, and SHA-256 hashing semantics now converge through `insforge-src/shared/canary-core.js`, `insforge-src/shared/debug-core.js`, and `insforge-src/shared/crypto-core.js`. CJS and ESM wrappers for `canary`, `debug`, and `crypto` are now thin export layers over those cores, and nullish hashing inputs are normalized identically across both runtimes.

UTC/local date-window normalization, timezone parsing, local date-key formatting, and usage max-days lookup now converge through `insforge-src/shared/date-core.js`. Request-id generation, function-name resolution, request/upstream logging, and slow-query threshold handling now converge through `insforge-src/shared/logging-core.js`. CJS and ESM wrappers for `date` and `logging` now provide only export-surface adaptation on top of those cores.

Shared pagination, rollup row fetching, daily bucket accumulation, canonical-model row filtering, and monthly bucket accumulation now converge through `insforge-src/shared/pagination-core.js`, `insforge-src/shared/usage-rollup-core.js`, `insforge-src/shared/usage-daily-core.js`, `insforge-src/shared/usage-filter-core.js`, and `insforge-src/shared/usage-monthly-core.js`. CJS wrappers in `shared/pagination.js`, `shared/usage-rollup.js`, and `shared/core/*` plus the ESM `usage-summary-support.js` and `shared/core/*` modules now delegate to those shared cores instead of carrying duplicate algorithms.

Shared user identity sanitization now converges through `insforge-src/shared/user-identity-core.js`, and leaderboard period/window math, snapshot token derivation, generated-at normalization, and public display/avatar normalization now converge through `insforge-src/shared/leaderboard-core.js`. CJS `vibeusage-leaderboard-refresh`, `vibeusage-leaderboard-profile`, `shared/user-identity.js`, and the ESM `vibeusage-leaderboard` function now consume those cores instead of keeping separate normalization paths.

Bucketed pricing resolution for aggregate usage endpoints now converges through `insforge-src/shared/usage-pricing-core.js`. The ESM `vibeusage-usage-summary`, `vibeusage-usage-daily`, and `vibeusage-usage-model-breakdown` functions now reuse the same alias-timeline pricing resolution, implied-model selection, and summary pricing-mode logic instead of maintaining duplicated cost-aggregation flows. `vibeusage-usage-model-breakdown` now attributes per-bucket costs back into source/model aggregates via the shared pricing core callback path, and `vibeusage-usage-summary` also now uses the shared source-parameter parser instead of a local duplicate.

Alias-timeline canonical-model matching for range-filtered usage queries now converges through `insforge-src/shared/usage-model-core.js`. The ESM `vibeusage-usage-hourly` and `vibeusage-usage-heatmap` functions now delegate their per-row canonical-model inclusion checks to the shared `matchesCanonicalModelAtDate` helper instead of carrying four inline copies of the same identity-comparison rule across UTC and local-time paths.

Row-level canonical-model filtering for aggregate usage endpoints now converges through `insforge-src/shared/usage-filter-core.js`. The shared `usage-monthly-core` and the ESM `vibeusage-usage-summary` function now reuse `shouldIncludeUsageRow` instead of each re-implementing `resolveIdentityAtDate` comparisons, which keeps alias-timeline effective-date filtering aligned across daily, monthly, and summary aggregation paths.

Raw hourly usage query construction now converges through `insforge-src/shared/usage-hourly-query-core.js`. The existing CJS `shared/db/usage-hourly.js` builder is now just a wrapper over that core, and ESM usage endpoints (`vibeusage-usage-summary`, `vibeusage-usage-daily`, `vibeusage-usage-monthly`, `vibeusage-usage-hourly`, `vibeusage-usage-heatmap`, and `vibeusage-usage-model-breakdown`) now all reuse the same `buildHourlyUsageQuery` helper for `vibeusage_tracker_hourly` reads instead of carrying duplicate `source/model/canary/range/order` query assembly logic in each function.

Request-time usage model filter context resolution now also converges through `insforge-src/shared/usage-model-core.js`. The shared `resolveUsageFilterContext` helper is exported through `functions-esm/shared/usage-summary-support.js`, and the ESM `vibeusage-usage-summary`, `vibeusage-usage-daily`, `vibeusage-usage-monthly`, `vibeusage-usage-hourly`, and `vibeusage-usage-heatmap` functions now all reuse that single `canonicalModel/usageModels/hasModelFilter/aliasTimeline` assembly path instead of repeating the same `resolveUsageModelsForCanonical + fetchAliasRows + buildAliasTimeline` orchestration block in each endpoint.

Aggregate pricing resolution for `vibeusage-usage-summary` and `vibeusage-usage-daily` now also converges through `insforge-src/shared/usage-pricing-core.js`. The shared `resolveAggregateUsagePricing` helper owns the repeated `identityMap/canonicalModels/impliedModelId/pricingProfile/summaryPricingMode/totalCostMicros` decision flow, so those ESM endpoints no longer carry separate copies of the same post-aggregation pricing logic.

Runtime aggregation selection for `vibeusage-usage-summary` and `vibeusage-usage-daily` now also converges onto the hourly-only path that is already enforced by `insforge-src/shared/usage-rollup-core.js`. The ESM `usage-summary-support` module no longer re-exports rollup helpers, and the `vibeusage-usage-summary` and `vibeusage-usage-daily` functions no longer carry dead `rollup flag / rollup fallback / split-range rollup` orchestration branches. Current production behavior is therefore expressed in exactly one place: rollup remains disabled in shared core, while the live endpoints always aggregate from hourly rows.

Alias-timeline resolution for usage-model sets now also converges through `insforge-src/shared/usage-model-core.js`. The new shared `resolveUsageTimelineContext` helper owns the repeated `normalize usageModels -> fetchAliasRows -> buildAliasTimeline` chain, so `resolveUsageFilterContext`, `insforge-src/shared/usage-pricing-core.js`, and the ESM `vibeusage-usage-model-breakdown` function no longer maintain separate copies of the same model-alias timeline assembly logic.

Hourly usage row pagination/orchestration now also converges through `insforge-src/shared/usage-hourly-query-core.js`. The shared `forEachHourlyUsagePage` helper owns the repeated `buildHourlyUsageQuery + forEachPage + normalized pageRows + rowCount` scan layer, so ESM usage endpoints (`vibeusage-usage-summary`, `vibeusage-usage-daily`, `vibeusage-usage-monthly`, `vibeusage-usage-hourly`, `vibeusage-usage-heatmap`, and `vibeusage-usage-model-breakdown`) no longer each carry their own copy of the hourly paging loop, and `functions-esm/shared/usage-summary-support.js` no longer re-exports generic pagination as a shadow source for hourly scans.

Aggregate usage ingest state for `vibeusage-usage-summary` and `vibeusage-usage-daily` now also converges through `insforge-src/shared/usage-pricing-core.js`. The shared `createAggregateUsageState` and `accumulateAggregateUsageRow` helpers now own the repeated `source/model normalization + billable fallback + totals/source entry mutation + pricing bucket accumulation` state machine, so those ESM endpoints no longer maintain separate copies of the same aggregate row-ingest flow before pricing resolution. Endpoint-specific logic remains limited to row filtering, rolling-window math, and response assembly.

Rolling-window usage aggregation for `vibeusage-usage-summary` now also converges through `insforge-src/shared/usage-pricing-core.js`. The shared `createRollingUsageState`, `accumulateRollingUsageRow`, and `buildRollingUsagePayload` helpers now own the repeated `billable fallback + rolling totals mutation + local active-day accounting + window summary` state machine, so the ESM summary endpoint no longer carries an inline copy of that row-ingest logic while still retaining ownership of rolling date-range selection and final response assembly.

Aggregate summary payload formatting for `vibeusage-usage-summary` and `vibeusage-usage-daily` now also converges through `insforge-src/shared/usage-pricing-core.js`. The shared `buildAggregateUsagePayload` helper now owns the repeated `model selection fields + stringified total counters + total_cost_usd formatting + pricing metadata assembly` logic, so those ESM endpoints no longer carry separate copies of the same response-payload formatting rules after aggregate pricing resolution. Endpoint-specific behavior remains limited to choosing whether the formatted payload is exposed as top-level summary fields or nested daily-summary fields.

Business-ready hourly row normalization now also converges through `insforge-src/shared/usage-row-core.js`. The shared `resolveHourlyUsageRowState` helper now owns the repeated `source normalization + billable fallback + normalized model key + effective date key + timestamp validation` prelude used before hourly usage rows are bucketed by the ESM `vibeusage-usage-hourly`, `vibeusage-usage-heatmap`, and `vibeusage-usage-model-breakdown` endpoints. Endpoint-specific behavior remains explicit: hourly and heatmap still require valid row timestamps, model-breakdown alone opts into a documented `effectiveDate` fallback when historical rows are replayed without `hour_start`, and aggregate/rolling pricing consumers opt into legacy default-source billable rules through an explicit helper flag rather than reintroducing inline duplicate math. The source-of-truth for row normalization therefore stays singular without silently changing endpoint contracts.

Heatmap payload assembly now also converges through `insforge-src/shared/usage-heatmap-core.js`. The shared `buildUsageHeatmapPayload` helper now owns the repeated `non-zero quantiles + activity counting + streak derivation + week-grid level mapping` logic that previously lived twice inside the UTC and local-time branches of `vibeusage-usage-heatmap`. The endpoint still owns request parsing, time-window calculation, and `valuesByDay` population, but the business rules for turning those day values into `thresholds`, `active_days`, `streak_days`, and `weeks` now have exactly one backend source of truth.

Heatmap request normalization and per-day accumulation now also converge through `insforge-src/shared/usage-heatmap-core.js`. The shared `normalizeHeatmapWeeks`, `normalizeHeatmapWeekStartsOn`, `normalizeHeatmapToDate`, and `accumulateHeatmapDayValue` helpers now own the repeated `weeks/week_starts_on/to` contract parsing and `valuesByDay` mutation rules that previously lived inline in `vibeusage-usage-heatmap`. The endpoint still owns auth, UTC-vs-local window selection, and hourly row scanning, but the business rules for validating heatmap request parameters and merging billable totals into day buckets now have exactly one backend source of truth.

Model-breakdown source/model aggregation now also converges through `insforge-src/shared/usage-pricing-core.js`. The shared `createModelBreakdownState`, `accumulateModelBreakdownRow`, `attributeModelBreakdownBucketCost`, and `buildModelBreakdownSources` helpers now own the repeated `source entry allocation + canonical model bucket allocation + per-source/per-model token mutation + attributed cost accumulation + totals formatting/sorting` logic that previously lived inline inside `vibeusage-usage-model-breakdown`. The endpoint still owns hourly row scanning, alias-effective identity resolution, and pricing bucket construction, but the business rules for turning those resolved rows into the public `sources[].totals` and `sources[].models[]` payload now have exactly one backend source of truth.

Aggregate summary payload assembly now also converges through `insforge-src/shared/usage-pricing-core.js`. The shared `buildAggregateUsagePayload` helper now owns the repeated `implied model selection + totals stringification + total_cost_usd formatting + pricing metadata assembly` logic that previously lived in parallel inside `vibeusage-usage-summary` and `vibeusage-usage-daily`. Those endpoints still own their different envelope shapes (`totals/pricing` at top level for summary, nested under `summary` for daily), but the business rules for producing the aggregate summary payload now have exactly one backend source of truth.

Hourly usage bucket assembly now also converges through `insforge-src/shared/usage-hourly-core.js`. The shared helpers now own the repeated `48 half-hour bucket creation + slot resolution + aggregate/raw token accumulation + response row rendering` logic that previously lived inline inside `vibeusage-usage-hourly`. The endpoint still owns auth, query selection, aggregate fallback, sync metadata lookup, and UTC/local time branching, but the business rules for turning hourly rows into public bucket payloads now have exactly one backend source of truth.

ISO normalization and sync-interval timing now also converge through `insforge-src/shared/date-core.js`. The shared `normalizeIso` and `isWithinInterval` helpers now own the repeated `timestamp trimming + ISO canonicalization + min-interval window check` logic that previously lived separately in `vibeusage-usage-hourly`, `vibeusage-user-status`, `vibeusage-sync-ping`, `shared/db/ingest`, and `shared/core/ingest`. Those consumers still own their surrounding auth, persistence, and response semantics, but the business rules for interpreting stored sync timestamps now have exactly one backend source of truth.

Usage token bucket payload formatting now also converges through `insforge-src/shared/usage-metrics-core.js`. The shared `buildUsageTotalsPayload` and `buildUsageBucketPayload` helpers now own the repeated `*_tokens` stringification contract that previously lived in parallel inside `usage-hourly-core`, `vibeusage-usage-daily`, `vibeusage-usage-monthly`, and `usage-pricing-core` aggregate/model-breakdown formatting paths. Endpoints still choose their envelope fields (`hour`, `day`, `month`, nested `totals`), but the backend contract for serializing token totals now has exactly one source of truth.

Local usage date-range request resolution now also converges through `insforge-src/shared/date-core.js`. The shared `resolveUsageDateRangeLocal` helper now owns the repeated `normalize local from/to -> enumerate day keys -> enforce max days -> derive UTC start/end bounds` contract that previously lived inline inside `vibeusage-usage-summary`, `vibeusage-usage-daily`, and `vibeusage-usage-model-breakdown`. Those endpoints still own auth, per-endpoint aggregation, and rolling-window behavior, but the backend contract for validating local usage ranges and converting them into hourly query bounds now has exactly one source of truth.

Project-usage summary aggregate semantics now also converge through `insforge-src/shared/project-usage-core.js`. The shared helpers now own the repeated `limit normalization + aggregate sum fallback detection + aggregate-row normalization + fallback all-rows aggregation/sorting` contract that previously lived inline inside `vibeusage-project-usage-summary`. The endpoint still owns auth, source filtering, query execution, and response envelope assembly, but the backend contract for interpreting aggregate project-usage results and producing fallback entries now has exactly one source of truth.

Aggregate hourly range collection for `vibeusage-usage-summary` and `vibeusage-usage-daily` now also converges through `insforge-src/shared/usage-aggregate-collector-core.js`. The dedicated shared `collectAggregateUsageRange` helper now owns the repeated `hourly query selection + canonical-model inclusion check + optional row admission gate + aggregate-state mutation + per-row callback` orchestration that previously lived in parallel inside both endpoints and briefly overlapped with `usage-pricing-core`. `usage-pricing-core` remains the single source of truth for aggregate pricing state mutation, while `vibeusage-usage-summary` still owns rolling-window scans and `vibeusage-usage-daily` still owns daily-bucket mutation. The backend contract for scanning aggregate hourly rows into shared pricing state now has exactly one source of truth.

Aggregate pricing payload resolution for `vibeusage-usage-summary` and `vibeusage-usage-daily` now also converges through `insforge-src/shared/usage-pricing-core.js`. The shared `resolveAggregateUsagePayload` helper now owns the repeated `aggregate state -> pricing summary resolution -> implied model selection -> totals/pricing payload assembly` flow that previously lived in parallel inside both endpoints. Those endpoints still own their final envelope shapes (`summary` nesting for daily, top-level totals for summary), but the backend contract for turning shared aggregate state into pricing-ready response payload fragments now has exactly one source of truth.

Debug-aware JSON response assembly for the usage-facing ESM endpoints now also converges through `insforge-src/shared/usage-response-core.js`. The shared `createUsageJsonResponder` helper now owns the repeated `debug query flag detection + slow-query debug payload merge + JSON response construction` flow that previously lived inline inside `vibeusage-usage-summary`, `vibeusage-usage-daily`, `vibeusage-usage-monthly`, `vibeusage-usage-hourly`, `vibeusage-usage-heatmap`, `vibeusage-usage-model-breakdown`, and `vibeusage-project-usage-summary`. Those endpoints still own their endpoint-specific payload envelopes and status selection, but the backend contract for turning a body/status/duration triple into the public JSON response now has exactly one source of truth.

Normalized hourly row collection for `vibeusage-usage-hourly`, `vibeusage-usage-heatmap`, and `vibeusage-usage-model-breakdown` now also converges through `insforge-src/shared/usage-row-collector-core.js`. The shared `collectHourlyUsageRows` helper now owns the repeated `forEachHourlyUsagePage -> resolveHourlyUsageRowState -> shouldIncludeUsageRow -> callback` orchestration that previously lived inline across those endpoints. The endpoints still own their accumulator semantics (`half-hour buckets`, `heatmap day values`, `rowsBuffer + pricing buckets`) and any endpoint-specific timestamp/day-key interpretation, but the backend contract for scanning paged hourly rows into normalized, canonically filtered callback inputs now has exactly one source of truth.

Aggregate request context for `vibeusage-usage-summary` and `vibeusage-usage-daily` now also converges through `insforge-src/shared/usage-aggregate-request-core.js`. The shared `resolveAggregateUsageRequestContext` helper now owns the repeated `source/model param parsing + local date-range normalization + aggregate filter-context resolution` flow that previously lived inline across both endpoints before they entered `collectAggregateUsageRange` and `resolveAggregateUsagePayload`. The endpoints still own bearer extraction, auth/access validation, slow-query logging, rolling-window behavior, daily bucket accumulation, and final response envelope shapes, but the backend contract for turning an already authorized request URL into aggregate-usage query context now has exactly one source of truth.

Source/model request parsing and canonical filter-context resolution for `vibeusage-usage-monthly`, `vibeusage-usage-hourly`, and `vibeusage-usage-heatmap` now also converge through `insforge-src/shared/usage-filter-request-core.js`. The shared `resolveUsageFilterRequestParams` and `resolveUsageFilterRequestContext` helpers now own the repeated `source/model param parsing + canonical-model filter-context resolution` flow that previously lived inline across those endpoints and inside `usage-aggregate-request-core`. The endpoints still own auth timing, time-window normalization, sync behavior, and final response envelope shapes, but the backend contract for turning usage request params into `source/model/canonicalModel/usageModels/aliasTimeline` query context now has exactly one source of truth.

Source plus local date-range request parsing for aggregate and model-breakdown usage endpoints now also converges through `insforge-src/shared/usage-range-request-core.js`. The shared `resolveUsageRangeRequestContext` helper now owns the repeated `source param parsing + local from/to normalization + day-key enumeration + UTC query-bound derivation` flow that previously lived inside `usage-aggregate-request-core` and `vibeusage-usage-model-breakdown`. `usage-aggregate-request-core` now composes that shared range core with `usage-filter-request-core`, while `vibeusage-usage-model-breakdown` reuses the same wrapper before entering row collection and pricing attribution. Auth, alias-timeline resolution, row scans, and endpoint envelope shapes stay explicit in the endpoints, but the backend contract for turning a usage request URL into `source/from/to/dayKeys/startIso/endIso` now has exactly one source of truth.

Heatmap request-window resolution now also converges through `insforge-src/shared/usage-heatmap-core.js`. The shared `resolveUsageHeatmapRequestContext` helper now owns the repeated `weeks/week_starts_on/to parsing + UTC/local grid window derivation + startIso/endIso query bound construction` flow that previously lived inline inside `vibeusage-usage-heatmap`. The endpoint still owns auth, canonical-model filter resolution, hourly row collection, and final payload emission, but the backend contract for turning a heatmap request URL into `weeks/weekStartsOn/from/to/gridStart/end/startIso/endIso` now has exactly one source of truth, eliminating the separate UTC and local window branches from the endpoint itself.

Project-usage aggregate and fallback query construction now also converges through `insforge-src/shared/project-usage-core.js`. The shared `buildProjectUsageAggregateQuery` and `buildProjectUsageFallbackQuery` helpers now own the repeated `table selection + user/source filters + canary exclusion + aggregate ordering/limit` flow that previously existed twice inside `vibeusage-project-usage-summary` for the primary aggregate query and the fallback raw-row query. The endpoint still owns source param parsing, fallback branching, slow-query logging, and final response envelope shape, but the backend contract for reading `vibeusage_project_usage_hourly` under those filters now has exactly one source of truth.

Hourly usage day-window and row-slot normalization now also converges through `insforge-src/shared/usage-hourly-core.js`. The shared `resolveUsageHourlyRequestContext` and `resolveUsageHourlyRowSlot` helpers now own the repeated `day parsing + UTC/local query-bound derivation + row timestamp to half-hour slot normalization` flow that previously lived in separate UTC and local branches inside `vibeusage-usage-hourly`. The endpoint still owns auth, timezone selection, the UTC-only aggregate fast-path, slow-query logging, and final payload emission, but the backend contract for turning an hourly request into `dayKey/startUtc/endUtc/startIso/endIso` and mapping scanned usage rows back into the requested half-hour bucket grid now has exactly one source of truth.

Usage filter request snapshot assembly now also converges through `insforge-src/shared/usage-filter-request-core.js`. The shared `resolveUsageFilterRequestSnapshot` helper now owns the repeated `source/model param parsing + canonical filter-context resolution` flow that previously remained split between `usage-aggregate-request-core` and the `vibeusage-usage-hourly`, `vibeusage-usage-monthly`, and `vibeusage-usage-heatmap` endpoints. Time windows, auth handling, collectors, and payload envelopes stay explicit in those consumers, but the backend contract for turning a usage request plus `effectiveDate` into `source/model/hasModelParam/canonicalModel/usageModels/hasModelFilter/aliasTimeline` now has exactly one source of truth.

Usage and project-usage endpoint preflight handling now also converges through `insforge-src/functions-esm/shared/core/usage-endpoint.js`. The shared `prepareUsageEndpoint`, `requireUsageAccess`, and `respondUsageRequestError` helpers now own the repeated `OPTIONS short-circuit + URL/responder creation + method gate + bearer extraction + auth failure mapping + request-validation error mapping` contract that previously lived inline across `vibeusage-usage-summary`, `vibeusage-usage-daily`, `vibeusage-usage-hourly`, `vibeusage-usage-heatmap`, `vibeusage-usage-monthly`, `vibeusage-usage-model-breakdown`, and `vibeusage-project-usage-summary`. Endpoint-specific request ordering still stays explicit, so handlers like heatmap and model-breakdown can continue validating windows before auth while summary/daily/hourly/monthly/project-usage authenticate earlier, but the backend contract for entering a usage endpoint now has exactly one source of truth.

Hourly detail and aggregate select contracts now also converge through `insforge-src/shared/usage-hourly-query-core.js`. The shared `DEFAULT_HOURLY_USAGE_SELECT`, `DETAILED_HOURLY_USAGE_SELECT`, and `AGGREGATE_HOURLY_USAGE_SELECT` constants now own the repeated `raw hourly detail columns + aggregate sum/count projection` strings that previously remained split across `usage-aggregate-collector-core`, `usage-row-collector-core`, `vibeusage-usage-summary`, `vibeusage-usage-monthly`, and `vibeusage-usage-hourly`. The collectors and endpoints still own their different scan paths and payload assembly, but the backend contract for which hourly columns must be fetched or aggregated now has exactly one source of truth, which reduces drift risk around `billable_total_tokens`, `cached_input_tokens`, and `reasoning_output_tokens`.

Monthly filtered hourly row collection now also converges through `insforge-src/shared/usage-row-collector-core.js`. The `vibeusage-usage-monthly` endpoint now reuses the same `collectHourlyUsageRows` path already used by hourly, heatmap, and model-breakdown, so raw hourly pagination, row-state normalization, and canonical-model inclusion checks no longer remain split between monthly and the other filtered usage endpoints. `usage-monthly-core` correspondingly stops re-running row filtering and billable fallback logic; it now consumes the normalized `usageRow` produced by the shared collector while still owning month-bucket initialization, month-key selection, and the monthly response envelope. This keeps the SSOT boundary tight: scan/filter/normalize live in the shared collector, while month-specific bucket assembly stays explicit in the monthly domain core.

Filtered usage endpoint scan/log orchestration now also converges through `insforge-src/functions-esm/shared/core/usage-filtered-rows.js`. The shared `collectFilteredUsageRows` helper now owns the repeated `collectHourlyUsageRows + query timing + slow-query logging` wrapper that previously remained duplicated across `vibeusage-usage-monthly`, `vibeusage-usage-heatmap`, `vibeusage-usage-model-breakdown`, and the raw-scan branches of `vibeusage-usage-hourly`. Those endpoints still own their request-context timing, accumulator callbacks, aggregate fast-paths, and final payload envelopes, but the ESM contract for scanning normalized filtered hourly rows and emitting consistent timing/log metadata now has exactly one source of truth.

Aggregate endpoint orchestration for `vibeusage-usage-summary` and `vibeusage-usage-daily` now also converges through `insforge-src/functions-esm/shared/core/usage-aggregate.js`. The shared `startAggregateUsageRequest` and `finishAggregateUsageRequest` helpers now own the repeated `resolveAggregateUsageRequestContext -> createAggregateUsageState -> collectAggregateUsageRange -> resolveAggregateUsagePayload` flow, plus the shared slow-query logging contract that surrounds it. The endpoints still own their real differences: `vibeusage-usage-summary` retains rolling-window assembly, and `vibeusage-usage-daily` retains daily bucket mutation and its nested response envelope. This keeps aggregate request execution singular without collapsing endpoint-specific payload behavior into a mega-core.

The ESM edge layer still contains additional duplicated business logic beyond this slice. This change converts immediately reachable contract drift to shared modules and guardrails first, then continues function-by-function backend convergence without changing public slugs.
